# Local development only. Generate real local values with:
# npm run db:local:start
# npm run env:local:sync
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<generated-local-anon-key>
