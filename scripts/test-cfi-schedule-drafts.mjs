import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import ts from 'typescript';

process.env.TZ = 'America/New_York';
const source = await readFile(new URL('../lib/cfi-schedule-drafts.ts', import.meta.url), 'utf8');
const compiled = ts.transpileModule(source, { compilerOptions: { module: ts.ModuleKind.ESNext, target: ts.ScriptTarget.ES2022 } }).outputText;
const { applyScheduleOperations, scheduleChanges, scheduleConflictsForLesson, scheduleHasOverlap, swapScheduleLessons } = await import(`data:text/javascript;base64,${Buffer.from(compiled).toString('base64')}`);
const lesson = (id, start, end, day = '2026-09-14') => ({ id, entry_type: 'lesson', student_user_id: id, student_name: id, lesson_kind: 'flight', aircraft_id:null, aircraft_tail_number:null, aircraft_status:null, aircraft_status_note:null, aircraft_conflict:false, start_at: `${day}T${start}:00-04:00`, end_at: `${day}T${end}:00-04:00`, status: 'scheduled', note: '', auto_generated: false, is_own: false });
const a = lesson('a', '07:00', '09:00');
const b = lesson('b', '09:30', '11:30');
const c = lesson('c', '12:00', '14:00');
const d = lesson('d', '09:00', '11:00', '2026-09-15');
const block = { ...lesson('block', '15:00', '16:00'), entry_type: 'unavailable', student_user_id: null };
const original = [a, b, c, d, block];
const edit = (entry, minutes) => ({ type: 'edit', id: entry.id, values: { lesson_kind: entry.lesson_kind, aircraft_id:entry.aircraft_id, note: entry.note, start_at: new Date(Date.parse(entry.start_at) + minutes * 60000).toISOString(), end_at: new Date(Date.parse(entry.end_at) + minutes * 60000).toISOString() } });
const middle = applyScheduleOperations(original, [edit(b, 30)]);
assert.equal(Date.parse(middle.find(e => e.id === 'a').start_at), Date.parse(a.start_at));
assert.equal(middle.find(e => e.id === 'c').start_at, c.start_at, 'editing a lesson never moves the next lesson');
assert.equal(middle.find(e => e.id === 'd').start_at, d.start_at);
assert.equal(middle.find(e => e.id === 'block').start_at, block.start_at);
assert.equal(Date.parse(middle.find(e => e.id === 'c').start_at) - Date.parse(middle.find(e => e.id === 'b').end_at), 0);
assert.deepEqual(scheduleConflictsForLesson(middle, 'b').map(e => e.id), [], 'touching endpoints do not overlap');
assert.equal(scheduleChanges(original, middle).length, 1);
const earlier = applyScheduleOperations(original, [edit(b, -15)]);
assert.equal(earlier.find(e => e.id === 'c').start_at, c.start_at);
assert.equal(scheduleHasOverlap(earlier), false);
const overlapsPrevious = applyScheduleOperations(original, [edit(b, -60)]);
assert.equal(scheduleHasOverlap(overlapsPrevious), true);
assert.deepEqual(scheduleConflictsForLesson(overlapsPrevious, 'b').map(e => e.id), ['a'], 'the previous affected lesson is identified');
const overlapsNext = applyScheduleOperations(original, [edit(b, 45)]);
assert.deepEqual(scheduleConflictsForLesson(overlapsNext, 'b').map(e => e.id), ['c'], 'the next affected lesson is identified');
const overlapsBoth = applyScheduleOperations(original, [{ type:'edit', id:'b', values:{...edit(b, -60).values, end_at:new Date(Date.parse(c.start_at) + 30 * 60000).toISOString()} }]);
assert.deepEqual(scheduleConflictsForLesson(overlapsBoth, 'b').map(e => e.id), ['a','c'], 'both sides are available for quick editing');
assert.equal(scheduleChanges(original, applyScheduleOperations(original, [edit(c, 30)])).length, 1);
assert.equal(scheduleChanges(original, applyScheduleOperations(original, [edit(a, 30)])).length, 1);
const cancelled = applyScheduleOperations(original, [{ type: 'cancel', id: 'b' }]);
assert.equal(scheduleChanges(original, cancelled)[0].after.status, 'cancelled');
const newLesson = lesson('new', '17:00', '19:00');
assert.equal(scheduleChanges(original, applyScheduleOperations(original, [{ type: 'add', entry: newLesson }, { type: 'cancel', id: 'new' }])).length, 0);
const latest = [...original, newLesson];
const rebased = applyScheduleOperations(latest, [edit(b, 30)]);
assert.equal(rebased.find(e => e.id === 'new').start_at, newLesson.start_at, 'replaying against a newer snapshot leaves new lessons untouched');
assert.throws(() => applyScheduleOperations(cancelled, [edit(b, 30)]), /removed or cancelled/);
const repeated = applyScheduleOperations(original, [edit(b, 30), edit({ ...b, ...edit(b, 30).values }, 30)]);
assert.equal(repeated.find(e => e.id === 'c').start_at, c.start_at);
const restored = applyScheduleOperations(original, [edit(b, 30), { type: 'edit', id: 'b', values: { start_at: b.start_at, end_at: b.end_at, note: '', lesson_kind: 'flight', aircraft_id:null } }]);
assert.equal(scheduleChanges(original, restored).length, 0);
const swapped = swapScheduleLessons(original, 'a', 'd');
assert.equal(Date.parse(swapped.find(e => e.id === 'a').start_at), Date.parse(d.start_at));
assert.equal(Date.parse(swapped.find(e => e.id === 'd').start_at), Date.parse(a.start_at));
assert.equal(Date.parse(swapped.find(e => e.id === 'a').end_at) - Date.parse(swapped.find(e => e.id === 'a').start_at), Date.parse(a.end_at) - Date.parse(a.start_at), 'a lesson keeps its duration');
assert.equal(swapped.find(e => e.id === 'a').lesson_kind, a.lesson_kind, 'lesson details stay with the student');
assert.equal(scheduleChanges(original, swapped).length, 2, 'a swap publishes exactly two edits');
assert.throws(() => swapScheduleLessons(original, 'a', 'a'), /different lessons/);
assert.throws(() => swapScheduleLessons([...original, { ...c, id:'same-student', student_user_id:'a' }], 'a', 'same-student'), /different students/);
// Pure scheduling checks: no database or network calls.
const scheduleSource = (await readFile(new URL('../lib/cfi-schedule.ts', import.meta.url), 'utf8'))
  .replace('import { getSupabaseClient } from "@/lib/supabase";', 'const getSupabaseClient = () => { throw new Error("Network access is forbidden in this test"); };');
const scheduleCompiled = ts.transpileModule(scheduleSource, { compilerOptions: { module: ts.ModuleKind.ESNext, target: ts.ScriptTarget.ES2022 } }).outputText;
const { availabilityEndToMinutes, defaultTeachingRules, generateAutomaticSchedule, getManualConflictWarnings, minutesToAvailabilityEnd } = await import(`data:text/javascript;base64,${Buffer.from(scheduleCompiled).toString('base64')}`);
assert.equal(availabilityEndToMinutes('00:00'),1440,'native midnight end time saves as the end of the day');
assert.equal(minutesToAvailabilityEnd(1440),'00:00','end-of-day availability loads into a valid native time input');
assert.equal(availabilityEndToMinutes('18:30'),1110,'ordinary end times keep their minute value');
const slots = [{ student_user_id:'a', scope:'weekly', weekday:1, start_minute:420, end_minute:900, timezone:'America/New_York' }];
const aircraftBlock = { id:'resource', cfi_user_id:'cfi', aircraft_id:'plane-1', start_at:a.start_at, end_at:a.end_at, note:'' };
const aircraft=[{id:'plane-1',tail_number:'N101PS',model_name:'C172',organization_id:null,operational_status:'available',operational_status_note:null,status_updated_at:null},{id:'plane-2',tail_number:'N202PS',model_name:'PA-28',organization_id:null,operational_status:'grounded',operational_status_note:'Inspection',status_updated_at:null}];
const warningInput = { studentUserId:'a', start:new Date(a.start_at), end:new Date(a.end_at), slots, overrideDates:[], blocks:[aircraftBlock] };
assert.equal(getManualConflictWarnings({...warningInput,lessonKind:'flight',aircraftId:'plane-1'}).length,1);
assert.equal(getManualConflictWarnings({...warningInput,lessonKind:'ground'}).length,0);
assert.match(getManualConflictWarnings({...warningInput,lessonKind:'flight',aircraftId:'plane-2',aircraftStatus:'grounded'})[0],/grounded/);
const autoInput = { cfiUserId:'cfi', weekStart:new Date('2026-09-14T12:00:00'), includeWeekends:false,
  access:[{student_user_id:'a',student_name:'Student',default_duration_min:120,default_weekly_sessions:1}],
  weekOverrides:[], slots, overrideDates:[], existingEntries:[], blocks:[aircraftBlock],aircraft,selectedAircraftIds:['plane-1'],aircraftReservations:[] };
const generated = generateAutomaticSchedule(autoInput).drafts;
assert.equal(generated.length,1);
assert.equal(Date.parse(generated[0].start_at),Date.parse(a.end_at),'automatic flight starts after the aircraft block');
assert.equal(generateAutomaticSchedule({...autoInput,blocks:[]}).drafts[0].start_at,new Date(a.start_at).toISOString(),'removing the block releases the earlier slot');
assert.equal(generateAutomaticSchedule({...autoInput,blocks:[],selectedAircraftIds:['plane-2']}).drafts.length,0,'unavailable aircraft are excluded even if a stale selection includes them');
const secondAccess={student_user_id:'b',student_name:'Second student',default_duration_min:120,default_weekly_sessions:4};
const selective=generateAutomaticSchedule({...autoInput,access:[...autoInput.access,secondAccess],slots:[...slots,{student_user_id:'b',scope:'weekly',weekday:1,start_minute:420,end_minute:900,timezone:'America/New_York'},{student_user_id:'b',scope:'weekly',weekday:2,start_minute:420,end_minute:900,timezone:'America/New_York'}],blocks:[],requests:[{studentUserId:'b',flightSessions:1,groundSessions:1}]});
assert.equal(selective.drafts.length,2,'the requested number controls additions instead of the saved weekly goal');
assert(selective.drafts.every(draft=>draft.student_user_id==='b'),'only selected students are scheduled');
assert.deepEqual(new Set(selective.drafts.map(draft=>draft.lesson_kind)),new Set(['flight','ground']),'Flight and Ground counts are independent');
assert.equal(new Set(selective.drafts.map(draft=>draft.start_at.slice(0,10))).size,2,'the same student is never auto-scheduled twice on one day');
const twoDaySlots=[...slots,{student_user_id:'a',scope:'weekly',weekday:2,start_minute:420,end_minute:900,timezone:'America/New_York'}];
const explicitAdditions=generateAutomaticSchedule({...autoInput,slots:twoDaySlots,blocks:[],existingEntries:[a],requests:[{studentUserId:'a',flightSessions:2,groundSessions:0}]});
assert.equal(explicitAdditions.drafts.length,1,'a weekly Flight total subtracts the student\'s existing Flight lesson');
assert.equal(explicitAdditions.drafts[0].start_at.slice(0,10),'2026-09-15','the missing lesson is placed on another day');
const metTarget=generateAutomaticSchedule({...autoInput,blocks:[],existingEntries:[a],requests:[{studentUserId:'a',flightSessions:1,groundSessions:0}]});
assert.equal(metTarget.drafts.length,0,'no duplicate is added when the weekly Flight total is already met');
const existingGround={...a,id:'ground-a',lesson_kind:'ground',aircraft_id:null,aircraft_tail_number:null};
const separateKinds=generateAutomaticSchedule({...autoInput,slots:[...twoDaySlots,{student_user_id:'a',scope:'weekly',weekday:3,start_minute:420,end_minute:900,timezone:'America/New_York'}],blocks:[],existingEntries:[a,existingGround],requests:[{studentUserId:'a',flightSessions:2,groundSessions:2}]});
assert.equal(separateKinds.drafts.filter((draft)=>draft.lesson_kind==='flight').length,1,'existing Flight is deducted only from the Flight total');
assert.equal(separateKinds.drafts.filter((draft)=>draft.lesson_kind==='ground').length,1,'existing Ground is deducted only from the Ground total');
const fairAccess=['fa','fb','fc'].map((id)=>({student_user_id:id,student_name:id.toUpperCase(),default_duration_min:120,default_weekly_sessions:3}));
const fairSlots=fairAccess.flatMap((student)=>[3,4,5].map((weekday)=>({student_user_id:student.student_user_id,scope:'weekly',weekday,start_minute:420,end_minute:540,timezone:'America/New_York'})));
const faMonday={...lesson('fa','07:00','09:00'),id:'fa-monday'};
const faTuesday={...lesson('fa','07:00','09:00','2026-09-15'),id:'fa-tuesday'};
const fairResult=generateAutomaticSchedule({...autoInput,access:fairAccess,slots:fairSlots,blocks:[],existingEntries:[faMonday,faTuesday],requests:fairAccess.map((student)=>({studentUserId:student.student_user_id,flightSessions:3,groundSessions:0}))});
const fairTotals=Object.fromEntries(fairAccess.map((student)=>[student.student_user_id,(student.student_user_id==='fa'?2:0)+fairResult.drafts.filter((draft)=>draft.student_user_id===student.student_user_id&&draft.lesson_kind==='flight').length]));
assert.equal(fairResult.drafts.filter((draft)=>draft.student_user_id==='fa').length,0,'students with more existing Flights wait while lower-count students receive scarce slots');
assert(Math.max(...Object.values(fairTotals))-Math.min(...Object.values(fairTotals))<=1,'final weekly Flight totals remain as even as available slots allow');
const compactAccess=[
  {student_user_id:'compact-a',student_name:'Alpha',default_duration_min:120,default_weekly_sessions:1},
  {student_user_id:'compact-b',student_name:'Bravo',default_duration_min:120,default_weekly_sessions:1},
  {student_user_id:'compact-c',student_name:'Charlie',default_duration_min:120,default_weekly_sessions:1},
];
const compactSlots=[
  {student_user_id:'compact-a',scope:'weekly',weekday:1,start_minute:420,end_minute:540,timezone:'America/New_York'},
  {student_user_id:'compact-b',scope:'weekly',weekday:1,start_minute:600,end_minute:780,timezone:'America/New_York'},
  {student_user_id:'compact-c',scope:'weekly',weekday:1,start_minute:540,end_minute:660,timezone:'America/New_York'},
];
const compactResult=generateAutomaticSchedule({...autoInput,access:compactAccess,slots:compactSlots,blocks:[],requests:compactAccess.map((student)=>({studentUserId:student.student_user_id,flightSessions:1,groundSessions:0}))});
assert.deepEqual(compactResult.drafts.map((draft)=>draft.student_user_id),['compact-a','compact-c','compact-b'],'an available student who closes the current gap is selected before a later start');
assert.deepEqual(compactResult.drafts.map((draft)=>new Date(draft.start_at).getHours()),[7,9,11],'automatic lessons run back-to-back when only one day is available');
const balancedSlots=compactAccess.flatMap((student)=>[1,2,3].map((weekday)=>({student_user_id:student.student_user_id,scope:'weekly',weekday,start_minute:420,end_minute:900,timezone:'America/New_York'})));
const balancedResult=generateAutomaticSchedule({...autoInput,access:compactAccess,slots:balancedSlots,blocks:[],requests:compactAccess.map((student)=>({studentUserId:student.student_user_id,flightSessions:1,groundSessions:0}))});
assert.deepEqual(balancedResult.drafts.map((draft)=>draft.start_at.slice(0,10)),['2026-09-14','2026-09-15','2026-09-16'],'equally available lessons fill separate days in order');
assert(balancedResult.drafts.every((draft)=>new Date(draft.start_at).getHours()===7),'the earliest compact time is still preferred within each selected day');
const cappedTotal=generateAutomaticSchedule({...autoInput,access:compactAccess,slots:compactSlots,blocks:[],teachingRules:{...defaultTeachingRules,max_daily_teaching_min:240},requests:compactAccess.map((student)=>({studentUserId:student.student_user_id,flightSessions:1,groundSessions:0}))});
assert.equal(cappedTotal.drafts.length,2,'daily total teaching minutes cap actual lesson durations');
const existingCap=generateAutomaticSchedule({...autoInput,access:[secondAccess],slots:[{student_user_id:'b',scope:'weekly',weekday:1,start_minute:540,end_minute:660,timezone:'America/New_York'}],blocks:[],existingEntries:[a],teachingRules:{...defaultTeachingRules,max_daily_teaching_min:120},requests:[{studentUserId:'b',flightSessions:1,groundSessions:0}]});
assert.equal(existingCap.drafts.length,0,'existing lessons consume the instructor daily total before adding a new lesson');
const cancelledEntry={...a,status:'cancelled'};
const ignoresCancelled=generateAutomaticSchedule({...autoInput,blocks:[],existingEntries:[cancelledEntry],requests:[{studentUserId:'a',flightSessions:1,groundSessions:0}]});
assert.equal(ignoresCancelled.drafts[0].start_at,new Date(a.start_at).toISOString(),'cancelled lessons do not consume time or weekly counts');
const unavailable=generateAutomaticSchedule({...autoInput,slots:[],blocks:[],requests:[{studentUserId:'a',flightSessions:1,groundSessions:0}]}).unscheduled[0];
assert.equal(unavailable.studentUserId,'a');
assert.equal(unavailable.scheduled,0);
assert.match(unavailable.reason,/No availability/);
const teachingRules={cfi_user_id:'cfi',start_minute:480,latest_start_minute:600,end_minute:720,max_daily_teaching_min:480,weekdays:[1]};
const laterSlot={student_user_id:'a',scope:'weekly',weekday:1,start_minute:540,end_minute:1020,timezone:'America/New_York'};
assert.equal(new Date(generateAutomaticSchedule({...autoInput,slots:[laterSlot],blocks:[]}).drafts[0].start_at).getHours(),9,'default rules keep later student start times available');
const taught=generateAutomaticSchedule({...autoInput,blocks:[],teachingRules}).drafts;
assert.equal(new Date(taught[0].start_at).getHours(),8,'instructor start time limits automatic scheduling');
assert.equal(generateAutomaticSchedule({...autoInput,blocks:[],teachingRules:{...teachingRules,end_minute:540}}).drafts.length,0,'instructor end time must accommodate the whole lesson');
const fourPmEnd=generateAutomaticSchedule({...autoInput,slots:[{...laterSlot,start_minute:960,end_minute:1080}],blocks:[],teachingRules:{...defaultTeachingRules,latest_start_minute:1439,end_minute:960}});
assert.equal(fourPmEnd.drafts.length,0,'a 16:00 teaching end never allows a 16:00–18:00 lesson even if the legacy latest-start value is later');
assert.equal(generateAutomaticSchedule({...autoInput,blocks:[],teachingRules:{...teachingRules,weekdays:[2]}}).drafts.length,0,'disabled teaching days are not used');
assert.equal(generateAutomaticSchedule({...autoInput,blocks:[],teachingRules:{...teachingRules,latest_start_minute:420}}).drafts.length,0,'latest start time limits automatic scheduling');
const wideWindowRules={...defaultTeachingRules,start_minute:420,latest_start_minute:960,end_minute:1080,max_daily_teaching_min:240,weekdays:[1]};
const wideWindow=generateAutomaticSchedule({...autoInput,access:[compactAccess[0],compactAccess[1]],slots:[compactSlots[0],{...compactSlots[1],start_minute:960,end_minute:1080}],blocks:[],teachingRules:wideWindowRules,requests:[{studentUserId:'compact-a',flightSessions:1,groundSessions:0},{studentUserId:'compact-b',flightSessions:1,groundSessions:0}]});
assert.equal(wideWindow.drafts.length,2,'the configured teaching window does not add a separate first-to-last span limit');
const ownTimeOff={id:'time-off',cfi_user_id:'cfi',start_at:a.start_at,end_at:a.end_at,note:'Vacation'};
assert.equal(generateAutomaticSchedule({...autoInput,blocks:[],instructorTimeOff:[ownTimeOff]}).drafts[0].start_at,new Date(a.end_at).toISOString(),'automatic scheduling skips instructor time off');
assert.equal(generateAutomaticSchedule({...autoInput,blocks:[],instructorTimeOff:[{...ownTimeOff,end_at:'2026-09-21T04:00:00Z'}]}).drafts.length,0,'a full-week vacation prevents automatic additions');
assert.equal(generateAutomaticSchedule({...autoInput,blocks:[],instructorTimeOff:[ownTimeOff],requests:[{studentUserId:'a',flightSessions:0,groundSessions:1}]}).drafts[0].start_at,new Date(a.end_at).toISOString(),'time off applies to Ground as well as Flight');
console.log('Automatic scheduling assertions passed, including instructor rules and time off.');
